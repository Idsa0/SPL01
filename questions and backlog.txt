TODO:
comments
empty lines on some command outputs and some not? should have emptylines on all.
ro5 move constructor in particular use the empty constructor clone() use std::move
input validation


tests:
step
everything else
when cd/dst  =0 the order is completed and moved on the same step


BUGS:


FORUM:
check it once in a while



Interest:

why does this make a memory leak:

void BackupWareHouse::act(WareHouse &wareHouse)
{
    extern WareHouse *backup;
    if (backup != nullptr)
        delete backup;

    backup = wareHouse.clone();
    delete backup;
    complete();
}

